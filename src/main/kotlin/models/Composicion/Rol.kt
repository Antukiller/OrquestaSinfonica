package antoine.lopez.models.Composicion

// Interface Rol que define un metodo general funcion que debe ser implementado por todas las clases que la heredan
interface Rol {

    // Metodo general que describe la función del rol
    fun funcion()
}